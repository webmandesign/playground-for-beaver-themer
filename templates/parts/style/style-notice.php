<?php
/**
 * CSS styles: Notice.
 *
 * @package    Playground for Beaver Themer
 * @copyright  WebMan Design, Oliver Juhas
 *
 * @since  1.1.0
 */

// Exit if accessed directly.
defined( 'ABSPATH' ) || exit;

?>

<style>

	.notice-beaver-themer {
		max-width: 52em;
		padding: calc( pow( var(--pfbt--ratio--base), 2 ) * 1em );
		margin: calc( pow( var(--pfbt--ratio--base), 2 ) * 1em ) auto;
		box-shadow: 0 calc( var(--pfbt--ratio--min) * 1em ) calc( pow( var(--pfbt--ratio--base), 2 ) * 1em ) rgba(0,0,0,.2);
	}

</style>
